# core-server-webhooks

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-webhooks` to build the library.

## Running unit tests

Run `nx test core-server-webhooks` to execute the unit tests via [Vitest](https://vitest.dev/).
