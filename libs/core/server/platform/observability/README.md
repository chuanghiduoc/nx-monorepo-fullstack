# core-server-observability

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-observability` to build the library.

## Running unit tests

Run `nx test core-server-observability` to execute the unit tests via [Vitest](https://vitest.dev/).
