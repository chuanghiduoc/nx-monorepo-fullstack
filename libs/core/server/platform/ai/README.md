# core-server-ai

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-ai` to build the library.

## Running unit tests

Run `nx test core-server-ai` to execute the unit tests via [Vitest](https://vitest.dev/).
