# core-server-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-core` to build the library.

## Running unit tests

Run `nx test core-server-core` to execute the unit tests via [Vitest](https://vitest.dev/).
