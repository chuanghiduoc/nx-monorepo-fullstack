# core-server-realtime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-realtime` to build the library.

## Running unit tests

Run `nx test core-server-realtime` to execute the unit tests via [Vitest](https://vitest.dev/).
