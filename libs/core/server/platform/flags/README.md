# core-server-flags

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-flags` to build the library.

## Running unit tests

Run `nx test core-server-flags` to execute the unit tests via [Vitest](https://vitest.dev/).
