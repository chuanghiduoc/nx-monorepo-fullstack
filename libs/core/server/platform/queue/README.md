# core-server-queue

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-queue` to build the library.

## Running unit tests

Run `nx test core-server-queue` to execute the unit tests via [Vitest](https://vitest.dev/).
