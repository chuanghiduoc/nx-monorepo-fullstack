# core-server-feature-auth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-feature-auth` to build the library.

## Running unit tests

Run `nx test core-server-feature-auth` to execute the unit tests via [Vitest](https://vitest.dev/).
