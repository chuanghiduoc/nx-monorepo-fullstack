# core-server-feature-realtime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-feature-realtime` to build the library.

## Running unit tests

Run `nx test core-server-feature-realtime` to execute the unit tests via [Vitest](https://vitest.dev/).
