# core-server-feature-ai

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-feature-ai` to build the library.

## Running unit tests

Run `nx test core-server-feature-ai` to execute the unit tests via [Vitest](https://vitest.dev/).
