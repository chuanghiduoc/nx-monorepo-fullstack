# core-server-feature-notes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-server-feature-notes` to build the library.

## Running unit tests

Run `nx test core-server-feature-notes` to execute the unit tests via [Vitest](https://vitest.dev/).
