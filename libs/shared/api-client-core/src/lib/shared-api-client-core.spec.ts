import { sharedApiClientCore } from './shared-api-client-core.js';

describe('sharedApiClientCore', () => {
  it('should work', () => {
    expect(sharedApiClientCore()).toEqual('shared-api-client-core');
  });
});
