# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc

# dependencies
node_modules

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

.claude/worktrees
.claude/settings.local.json
.nx/polygraph
.nx/self-healing
.nx/migrate-runs
vitest.config.*.timestamp*
dist
out-tsc
test-output

# Next.js
.next
out
vite.config.*.timestamp*

# Prisma CLI installs agent skill documentation next to the schema. It is
# vendor documentation, not project source; `prisma init` recreates it.
libs/**/.agents/
libs/**/.claude/
libs/**/.windsurf/
libs/**/skills-lock.json

# Local configuration. The `*.example` files are the tracked templates; the
# files they are copied to hold whatever this machine actually uses, including
# real secrets, and must never be committed.
.env
.env.*
!.env.example
!.env.prod.example
