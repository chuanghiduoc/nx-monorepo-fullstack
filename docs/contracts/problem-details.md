# Contract: problem details

> **Status:** to be written in **Phase 2**.

RFC 9457 profile: the error type registry, how Zod issues map to `errors[]`, and how `traceId` is attached.

Until then the authoritative description lives in
`docs/superpowers/specs/2026-09-01-nx-fullstack-boilerplate-design.md`.
This file exists so the contract has an address before it has content — a
contract that is only in a design document tends to stay there.
