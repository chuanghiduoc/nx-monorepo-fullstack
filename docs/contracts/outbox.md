# Contract: outbox

> **Status:** to be written in **Phase 4**.

State machine (PENDING → PROCESSING → ENQUEUED → DEAD), relay claiming with SKIP LOCKED, consumer dedup and the replay contract.

Until then the authoritative description lives in the architecture design
under `docs/superpowers/specs/`. This file exists so the contract has an
address before it has content — a contract that lives only in a design
document tends to stay there.
