{
  "$schema": "https://json-schema.org/schema",
  "$id": "ProductScope",
  "title": "Prepare a new product scope",
  "description": "Writes an ordered checklist of the CLI commands and boundary-rule change needed to add a product scope (apps/<name>/*).",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Scope name, e.g. 'crm' or 'hrm'.",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the new product scope called?"
    }
  },
  "required": ["name"]
}
